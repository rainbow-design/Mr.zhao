// 不支持es6，Date

function splitByC(str) {
  return str.split(',');
}
function splitByS(str) {
  return str.split(' ');
}

module.exports = {
  splitByC: splitByC, // my_coupon 有使用
  splitByS: splitByS
}